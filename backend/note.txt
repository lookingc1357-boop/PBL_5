{
    "signature": "main()",
    "position": {
        "start_line": 7,
        "start_col": 0,
        "end_line": 54,
        "end_col": 1
    },
    "code": "int main() {\r\n    int server_fd, new_socket;\r\n    struct sockaddr_in address;\r\n    int opt = 1;\r\n    int addrlen = sizeof(address);\r\n    char buffer[1024] = {0};\r\n    const char* hello = \"Xin chao tu Server TCP!\";\r\n\r\n    // 1. Tao Socket file descriptor (IPv4, TCP)\r\n    if ((server_fd = socket(AF_INET, SOCK_STREAM, 0)) == 0) {\r\n        perror(\"Socket that bai\");\r\n        exit(EXIT_FAILURE);   \r\n    }\r\n\r\n    // 2. Gan cong (Port) 8080 vao Socket\r\n    address.sin_family = AF_INET;\r\n    address.sin_addr.s_addr = INADDR_ANY; // Chap nhan moi IP den\r\n    address.sin_port = htons(8080);\r\n\r\n    if (bind(server_fd, (struct sockaddr*)&address, sizeof(address)) < 0) {\r\n        perror(\"Bind that bai\");\r\n        exit(EXIT_FAILURE);\r\n    }\r\n\r\n    // 3. Lang nghe ket noi (Toi da 3 ket noi doi)\r\n    if (listen(server_fd, 3) < 0) {\r\n        perror(\"Listen that bai\");\r\n        exit(EXIT_FAILURE);\r\n    }\r\n\r\n    std::cout << \"Server dang doi o port 8080...\" << std::endl;\r\n\r\n    // 4. Chap nhan ket noi tu Client\r\n    if ((new_socket = accept(server_fd, (struct sockaddr*)&address, (socklen_t*)&addrlen)) < 0) {\r\n        perror(\"Accept that bai\");\r\n        exit(EXIT_FAILURE);\r\n    }\r\n\r\n    // 5. Doc du lieu va gui phan hoi\r\n    read(new_socket, buffer, 1024);\r\n    std::cout << \"Client gui: \" << buffer << std::endl;\r\n    send(new_socket, hello, strlen(hello), 0);\r\n    std::cout << \"Da gui phan hoi cho Client\" << std::endl;\r\n\r\n    close(new_socket);\r\n    close(server_fd);\r\n    return 0;\r\n}"
}